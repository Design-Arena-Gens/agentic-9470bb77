export const metadata = {
  title: "Castle Attack",
  description: "Build a castle and survive attacks",
};

import "./globals.css";

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
